---
title: Agent outputs
description: Where agents write files and how run directories mirror workspace output.
---

Faraday agents write generated artifacts under `./agent_outputs/` in the workspace (with subfolders such as `plots/`, `data/`, and `webpages/` as needed). Each run also records outputs under the run directory’s `agent_outputs/` folder.
